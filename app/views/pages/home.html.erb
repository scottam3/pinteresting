<div class="jumbotron">
	<h1>Welcome to Scott's App</h1>
	<% if user_signed_in? %>
		# do something
		<% else %>

		<p><%= link_to "Sign up ", new_user_registration_path,class: 'btn btn-primary btn-lg' %></p>
		<p><%= link_to "Login", new_user_session_path,class: 'btn btn-primary btn-lg' %></p>
		<% end %>
</div>
